// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: dashboard.sql

package repository

import (
	"context"
	"time"
)

const countProductsByDefaultApprovalStatus = `-- name: CountProductsByDefaultApprovalStatus :many
SELECT default_approval_status, count(*) AS product_count
FROM products
GROUP BY default_approval_status
ORDER BY default_approval_status
`

type CountProductsByDefaultApprovalStatusRow struct {
	DefaultApprovalStatus string
	ProductCount          int64
}

// CountProductsByDefaultApprovalStatus returns the number of products
// per default_approval_status for the dashboard approval summary
// widget.
func (q *Queries) CountProductsByDefaultApprovalStatus(ctx context.Context) ([]CountProductsByDefaultApprovalStatusRow, error) {
	rows, err := q.db.QueryContext(ctx, countProductsByDefaultApprovalStatus)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []CountProductsByDefaultApprovalStatusRow
	for rows.Next() {
		var i CountProductsByDefaultApprovalStatusRow
		if err := rows.Scan(&i.DefaultApprovalStatus, &i.ProductCount); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listDeactivatedUserAssignments = `-- name: ListDeactivatedUserAssignments :many
SELECT u.id, u.name, u.department_id, p.canonical_name, ua.assigned_at
FROM user_assignments ua
JOIN users u ON u.id = ua.user_id
JOIN licenses l ON l.id = ua.license_id
JOIN products p ON p.id = l.product_id
WHERE ua.revoked_at IS NULL AND u.deactivated_at IS NOT NULL
ORDER BY u.department_id
`

type ListDeactivatedUserAssignmentsRow struct {
	ID            int64
	Name          string
	DepartmentID  *int64
	CanonicalName string
	AssignedAt    time.Time
}

// ListDeactivatedUserAssignments returns active user assignments that
// still point at deactivated users (spec section 5.14, SQL taken
// verbatim). Feeds the dashboard leaver widget.
func (q *Queries) ListDeactivatedUserAssignments(ctx context.Context) ([]ListDeactivatedUserAssignmentsRow, error) {
	rows, err := q.db.QueryContext(ctx, listDeactivatedUserAssignments)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListDeactivatedUserAssignmentsRow
	for rows.Next() {
		var i ListDeactivatedUserAssignmentsRow
		if err := rows.Scan(
			&i.ID,
			&i.Name,
			&i.DepartmentID,
			&i.CanonicalName,
			&i.AssignedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listExpiringLicenses = `-- name: ListExpiringLicenses :many
SELECT
  l.id, l.display_name, l.expires_at, l.total_count, l.count_unit,
  p.canonical_name AS product_name,
  ve.name AS vendor_name,
  d.name AS department_name
FROM licenses l
JOIN products p ON p.id = l.product_id
JOIN vendors ve ON ve.id = p.vendor_id
JOIN departments d ON d.id = l.owning_department_id
WHERE l.expires_at IS NOT NULL
  AND l.expires_at >= date('now')
  AND l.expires_at < date('now', '+91 days')
ORDER BY l.expires_at, l.id
`

type ListExpiringLicensesRow struct {
	ID             int64
	DisplayName    string
	ExpiresAt      *time.Time
	TotalCount     *int64
	CountUnit      string
	ProductName    string
	VendorName     string
	DepartmentName string
}

// ListExpiringLicenses returns active licenses expiring within 90 days
// for the dashboard expiring-licenses widget, soonest first. The lower
// bound matches ListLicenses' active condition (expires_at >=
// date('now')). The upper bound uses < date('now', '+91 days') instead
// of <= date('now', '+90 days') because expires_at values written by
// the Go driver carry a time-of-day suffix that would sort after the
// bare date string, silently excluding day 90.
func (q *Queries) ListExpiringLicenses(ctx context.Context) ([]ListExpiringLicensesRow, error) {
	rows, err := q.db.QueryContext(ctx, listExpiringLicenses)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListExpiringLicensesRow
	for rows.Next() {
		var i ListExpiringLicensesRow
		if err := rows.Scan(
			&i.ID,
			&i.DisplayName,
			&i.ExpiresAt,
			&i.TotalCount,
			&i.CountUnit,
			&i.ProductName,
			&i.VendorName,
			&i.DepartmentName,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listLicenseUsage = `-- name: ListLicenseUsage :many

SELECT
  u.product_id,
  u.canonical_name,
  u.vendor_name,
  u.total_owned,
  u.installed_count,
  u.user_assigned_count,
  u.device_assigned_count,
  EXISTS(
    SELECT 1 FROM licenses l
    WHERE l.product_id = u.product_id
      AND l.total_count IS NULL
      AND (l.expires_at IS NULL OR l.expires_at > date('now'))
  ) AS has_unlimited
FROM v_license_usage u
ORDER BY u.vendor_name, u.canonical_name
`

type ListLicenseUsageRow struct {
	ProductID           int64
	CanonicalName       string
	VendorName          string
	TotalOwned          int64
	InstalledCount      int64
	UserAssignedCount   int64
	DeviceAssignedCount int64
	HasUnlimited        bool
}

// Comments in this file must stay ASCII-only; sqlc v1.31.1 misparses
// non-ASCII comments and splits queries incorrectly.
// ListLicenseUsage returns the company-wide per-product usage summary
// for the dashboard shortage/surplus widget. Columns come from the
// v_license_usage view; has_unlimited is added because the view folds
// NULL total_count (= unlimited) licenses into total_owned as 0, which
// would make an unlimited product look over-allocated. The expiry
// condition matches the view's active-license condition
// (expires_at > date('now')).
func (q *Queries) ListLicenseUsage(ctx context.Context) ([]ListLicenseUsageRow, error) {
	rows, err := q.db.QueryContext(ctx, listLicenseUsage)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListLicenseUsageRow
	for rows.Next() {
		var i ListLicenseUsageRow
		if err := rows.Scan(
			&i.ProductID,
			&i.CanonicalName,
			&i.VendorName,
			&i.TotalOwned,
			&i.InstalledCount,
			&i.UserAssignedCount,
			&i.DeviceAssignedCount,
			&i.HasUnlimited,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
